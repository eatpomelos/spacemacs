(defun open-my-list ()
  "open my list file."
  (interactive)
  (find-file "~/pomelo/org/dairy.org"))

(defun open-my-note ()
  "open note file."
  (interactive)
  (find-file "~/pomelo/org/note.org"))

(defun open-my-learning ()
  "open note file."
  (interactive)
  (find-file "~/pomelo/org/learning.org"))
